<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Котики</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
</head>
<body>
    <div class="container text-center mt-5">
        <h1 class="display-4">Случайный котик</h1>
        <div class="cat-image mt-4">
            {% if cat_image %}
                <img src="{{ cat_image }}" alt="Random Cat" class="img-fluid rounded shadow">
            {% else %}
                <p class="text-danger">Не удалось загрузить котика :(</p>
            {% endif %}
        </div>
        <a href="/" class="btn btn-primary mt-4">Еще котик!</a>
    </div>
</body>
</html>
